#!/bin/bash

echo "═══════════════════════════════════════════════════════════"
echo "🔍 DIAGNOSTIC COMPLET - Loookaa Spirit"
echo "═══════════════════════════════════════════════════════════"
echo ""

echo "📂 1. Vérification des fichiers dans /spirit/"
echo "───────────────────────────────────────────────────────────"
ls -la ~/loookaa/spirit/ | head -20
echo ""

echo "📄 2. Vérification index.html"
echo "───────────────────────────────────────────────────────────"
if [ -f ~/loookaa/spirit/index.html ]; then
    echo "✅ index.html existe"
    ls -lh ~/loookaa/spirit/index.html
else
    echo "❌ index.html N'EXISTE PAS !"
fi
echo ""

echo "🔧 3. Contenu du .htaccess de /spirit/"
echo "───────────────────────────────────────────────────────────"
cat ~/loookaa/spirit/.htaccess
echo ""

echo "🔧 4. Contenu du .htaccess WordPress (racine)"
echo "───────────────────────────────────────────────────────────"
cat ~/loookaa/.htaccess | head -30
echo ""

echo "🌐 5. Test HTTP direct sur le serveur (localhost)"
echo "───────────────────────────────────────────────────────────"
curl -I http://localhost/spirit/ 2>&1 | head -10
echo ""

echo "🌐 6. Test HTTPS externe avec cache-busting"
echo "───────────────────────────────────────────────────────────"
curl -I "https://loookaa.com/spirit/?nocache=$(date +%s)" 2>&1 | head -15
echo ""

echo "🔌 7. Statut PM2"
echo "───────────────────────────────────────────────────────────"
pm2 status
echo ""

echo "🔌 8. Test API en local"
echo "───────────────────────────────────────────────────────────"
curl -s http://localhost:3002/api/settings | head -5
echo ""

echo "📊 9. Permissions du dossier spirit"
echo "───────────────────────────────────────────────────────────"
ls -ld ~/loookaa/spirit
echo ""

echo "📊 10. Permissions de index.html"
echo "───────────────────────────────────────────────────────────"
ls -l ~/loookaa/spirit/index.html 2>&1
echo ""

echo "🔗 11. Vérification du symlink (si existe)"
echo "───────────────────────────────────────────────────────────"
ls -la ~ | grep -E "(public_html|www)"
echo ""

echo "🔍 12. Vérification Apache est actif"
echo "───────────────────────────────────────────────────────────"
ps aux | grep httpd | head -3
echo ""

echo "═══════════════════════════════════════════════════════════"
echo "✅ Diagnostic terminé"
echo "═══════════════════════════════════════════════════════════"
